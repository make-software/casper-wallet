npx lint-staged
# The whole CI gate, e2e aside. It was static-checks-only while the unit suite
# cost minutes; it now runs in well under one (see jest.config.js, tsconfig.jest.json).
npm run ci-check
