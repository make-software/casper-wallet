import styled, { createGlobalStyle } from 'styled-components';

export const ContainerBackground = createGlobalStyle`
  body {
    background-color: #242424;
  }
`;

export const Container = styled.div`
  color: #ffffff;
`;
