export enum Browsers {
  firefox = 'firefox',
  chrome = 'chrome'
}

export enum ElementState {
  visible = 'visible',
  detached = 'detached',
  hidden = 'hidden'
}

export enum ExtensionBuildPath {
  chrome = 'build/chrome',
  firefox = 'build/firefox'
}

export const e2eConfigFilename = 'e2e.tsconfig.json';

export const extensionName = 'CasperLabs Signer';
